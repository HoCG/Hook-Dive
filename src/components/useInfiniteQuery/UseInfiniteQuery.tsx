import React from 'react';
import { useInfiniteQuery } from 'react-query';

const UseInfiniteQuery = () => {
  //대용량 데이터가 있어야 확인하는 맛이 좀 나는 useInfiniteQuery... 지금은 가지고있는 데이터의 양이 많이없어서 사용에 제약이 걸린다. 나중에 기회가 된다면 이 훅을 활용해서
  //재밌는 화면을 만들어보도록 하자.
  return (
    <div>
      
    </div>
  );
};

export default UseInfiniteQuery;